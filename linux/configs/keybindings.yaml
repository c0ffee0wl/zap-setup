# Zap keybindings mirroring the effective Terminator set after
# /opt/linux-setup/linux-setup.sh is applied — Terminator defaults
# UNION linux-setup overrides MINUS explicit unbinds — plus a few
# Zap-specific unbinds the live config writes.
#
# Keymap engine notes:
# - Valid modifiers are exactly: ctrl, alt, shift, cmd, meta, cmdorctrl
#   (crates/warpui_core/src/keymap.rs; parsed by Keystroke::parse via
#   app/src/keyboard.rs). `cmd-*` is the cross-platform primary modifier;
#   on Linux it maps to the Super key. There is NO `logo`/`super` token —
#   Keystroke::parse rejects it, so a binding like `ctrl-shift-logo-D` silently
#   fails to load.
# - `shift+letter` must be uppercase (ctrl-shift-N, not ctrl-shift-n).
# - `none` removes a binding.
#
# Environment caveats:
# - XFCE grabs some Super chords globally (before the focused app), so they
#   never reach Zap. rename_active_tab is on cmd-q (Super+Q); XFCE has no
#   default there, so setup.sh Phase 5 clears any stray Super+Q binding for you
#   (we deliberately leave Super+E alone so XFCE's file-manager shortcut keeps
#   working). The split chords (Super+Y/Super+A) plus the bare-Super Whisker
#   grab (Super_L, which on XFCE < 4.20 swallows every Super+<letter> chord) are
#   reported by Phase 5 — remove the ones you want Zap to own in Settings ->
#   Keyboard -> Application Shortcuts, otherwise those cmd-* (Super) chords
#   won't reach Zap.
---

# Font / zoom (Terminator defaults: Ctrl++ / Ctrl+- / Ctrl+0).
# Three parallel action triplets all bound to the same chord — Zap
# dispatches to whichever view owns focus (terminal font, workspace UI
# scale, notebook font).
"workspace:increase_font_size":                    ctrl-+
"workspace:decrease_font_size":                    ctrl--
"workspace:reset_font_size":                       ctrl-0
"workspace:increase_zoom":                         ctrl-+
"workspace:decrease_zoom":                         ctrl--
"workspace:reset_zoom":                            ctrl-0
"notebookview:increase_font_size":                 ctrl-+
"notebookview:decrease_font_size":                 ctrl--
"notebookview:reset_font_size":                    ctrl-0

# Tabs (linux-setup overrides: Ctrl+T, Ctrl+Tab, Ctrl+1..6, Super+Q)
"workspace:new_tab":                               ctrl-t
"workspace:new_window":                            ctrl-shift-I
"workspace:close_active_tab":                      none
# Terminator default close_window = Ctrl+Shift+Q. (Previously `ctrl-shift-logo-D`,
# but `logo` is not a valid Zap modifier, so that binding never loaded.)
"workspace:close_window":                          ctrl-shift-Q
"workspace:activate_next_tab":                     ctrl-tab
"workspace:activate_prev_tab":                     ctrl-shift-tab
"workspace:activate_first_tab":                    ctrl-1
"workspace:activate_second_tab":                   ctrl-2
"workspace:activate_third_tab":                    ctrl-3
"workspace:activate_fourth_tab":                   ctrl-4
"workspace:activate_fifth_tab":                    ctrl-5
"workspace:activate_sixth_tab":                    ctrl-6
"workspace:activate_seventh_tab":                  none
"workspace:activate_eighth_tab":                   none
"workspace:move_tab_right":                        ctrl-shift-pagedown
"workspace:move_tab_left":                         ctrl-shift-pageup
"workspace:rename_active_tab":                     cmd-q

# Panes / splits (linux-setup: Super+Y down / Super+A right; defaults for nav + resize)
"pane_group:add_down":                             cmd-y
"pane_group:add_right":                            cmd-a
"pane_group:add_up":                               none
"pane_group:add_left":                             none
"pane_group:close_current_session":                none
"pane_group:navigate_up":                          alt-up
"pane_group:navigate_down":                        alt-down
"pane_group:navigate_left":                        alt-left
"pane_group:navigate_right":                       alt-right
"pane_group:navigate_next":                        ctrl-shift-N
"pane_group:navigate_prev":                        ctrl-shift-P
"pane_group:resize_up":                            ctrl-shift-up
"pane_group:resize_down":                          ctrl-shift-down
"pane_group:resize_left":                          ctrl-shift-left
"pane_group:resize_right":                         ctrl-shift-right
"pane_group:toggle_maximize_pane":                 ctrl-shift-X

# Window
"root_view:toggle_fullscreen":                     f11
"workspace:hide_warp":                             ctrl-shift-alt-A
# Left panel (Warp Drive: sessions / agents / files). Zap ships this action
# UNBOUND — the title-bar "Search sessions, agents, files..." click is the only
# built-in way in — so bind it to match the live host config. ctrl-shift-> is
# German-QWERTZ-reachable: `>` is Shift on the <>| key left of Z. (NB: Zap's
# default also maps ctrl-shift-> to terminal:ask_ai_assistant_last_block, but
# that binding is gated off when Agent Mode is enabled, so no clash in practice.)
"workspace:toggle_left_panel":                     ctrl-shift->
# Right panel — labelled "Toggle code review" in Settings (it hosts the
# code-review / diff view). Zap's default ctrl-shift-+ is unreachable on a German
# layout (`+` is unshifted; Shift yields `*`), so bind to ctrl-shift-* (`*` is
# Shift on the + key) to match the live host config.
"workspace:toggle_right_panel":                    ctrl-shift-*

# Search / edit (linux-setup override: Ctrl+F instead of Ctrl+Shift+F).
# The three *_find actions are bound to the same chord so Ctrl+F opens
# search in the terminal, the editor pane, and the code-editor pane alike.
"terminal:find":                                   ctrl-f
"editor:find":                                     ctrl-f
"code_editor:find":                                ctrl-f
"terminal:copy":                                   ctrl-shift-C
"terminal:paste":                                  ctrl-shift-V

# Terminal reset (mirrors Terminator's default `reset = Ctrl+Shift+R`;
# Zap has only the one clear action, no separate reset_clear analogue).
"terminal:clear_blocks":                           ctrl-shift-R

# Beyond Terminator (productivity add, reachable because linux-setup
# moved search off Ctrl+Shift+F onto Ctrl+F). Action is open_global_search —
# show_global_search is not a real Zap action and silently fails to load.
"workspace:open_global_search":                    ctrl-shift-F

# Settings
"workspace:show_settings_keyboard_shortcuts_page": ctrl-shift-K

# Zap-specific defaults we explicitly disable (these have no
# Terminator analogue and the user doesn't want them firing on stray
# keystrokes). Kept in the file so re-runs don't see them re-appear.
"editor_view:underline":                           none
"editor_view:right":                               none
"terminal:select_bookmark_up":                     none
"terminal:select_bookmark_down":                   none
"ai_assistant_panel:reset_context":                none
"code_view:close_saved_tabs":                      none
"code_view:close_all_tabs":                        none
