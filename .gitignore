# Claude Code local state
.claude

# Local planning/notes — kept out of the repo (see docs/PLAN.md's header)
PLAN*
docs/superpowers/

# Timestamped backups the installers write when run against a checkout copy
*.backup.*
